name: Bootstrap Partcounter R001.25 Hardening (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "R001.25 source hardening has already been applied. Use build-r00125.yml for validation."
